//List of Enums
//they can't be in class

enum TexJSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  googlepay,
  stripe,
  applepay,
  visa,
  mastercard,
  paytm,
}
